import React from 'react';

const Form = (props) => {
    return (
        <form>
            <label>Tu Nombre</label>
            <input type="text" placeholder="Ingrese su nombre"/>
        </form>
    );
}

export default Form;
